# PostgreSQL — Vercel Postgres / Neon / Supabase
DATABASE_URL=postgresql://usuario:senha@host:5432/omv?sslmode=require

# JWT — gere com: node -e "console.log(require('crypto').randomBytes(48).toString('base64'))"
JWT_SECRET=troque-este-valor-por-um-segredo-com-minimo-32-caracteres

# Opcional: porta local
# PORT=3000
